import {createTheme} from "@material-ui/core";

const Palette = createTheme({
    palette: {
        primary: {
            main: "#57CC99",
        },
        secondary: {
            main: "#FFEEDB"
        },
        ternary: {
            main: "#202C39"
        }
    }
})
export default Palette